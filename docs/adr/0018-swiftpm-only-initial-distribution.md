# SwiftPM-only Initial Distribution

The initial Swift binding distribution will support Swift Package Manager only. SwiftPM provides the native path for Swift 6 source targets, tests, DocC, and checksumed binary targets, while CocoaPods and Carthage would add packaging work before the core FFI, XCFramework, and API risks are resolved.
